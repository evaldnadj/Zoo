
export class Animal {
    species: string;
    name: string;
    date_of_birth: string;

    constructor(species: string, name: string, date_of_birth: string)
    {
        this.species=species;
        this.name=name;
        this.date_of_birth=date_of_birth;
    }
}